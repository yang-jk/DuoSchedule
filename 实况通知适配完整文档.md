# Android 15 实况通知 (Promoted Notifications) 完整适配文档

## 一、项目概述

### 1.1 项目信息

| 属性 | 值 |
|------|------|
| 项目名称 | AppShare |
| 包名 | info.muge.appshare |
| 版本号 | 5.0.8 (versionCode: 403) |
| minSdkVersion | 26 (Android 8.0) |
| targetSdkVersion | 36 (Android 15) |
| compileSdkVersion | 36 |
| Gradle Plugin | 8.10.1 |
| Java版本 | 1.8 |

### 1.2 项目结构

```
d:\appshare/
├── app/
│   ├── src/
│   │   └── main/
│   │       ├── assets/
│   │       │   └── na.czl
│   │       └── java/
│   │           ├── p/                          # 混淆后的Kotlin代码
│   │           │   ├── nf1.java               # 下载进度通知构建器
│   │           │   ├── mf1.java               # 多任务下载通知构建器
│   │           │   ├── of1.java               # 通知渠道管理器
│   │           │   ├── am3.java               # 设置界面逻辑
│   │           │   ├── ve2.java               # 用户设置存储
│   │           │   └── ...
│   │           ├── info/muge/appshare/
│   │           │   ├── service/
│   │           │   │   ├── DownloadForegroundService.java  # 下载前台服务
│   │           │   │   └── UploadWorker.java
│   │           │   └── utils/download/core/
│   │           │       └── DownloadTaskManager.java       # 下载任务管理
│   │           └── androidx/core/app/
│   │               ├── NotificationCompat.java            # 通知兼容库
│   │               ├── NotificationCompatBuilder.java     # 通知构建器
│   │               └── NotificationManagerCompat.java     # 通知管理器兼容
│   └── build.gradle
├── build.gradle
├── settings.gradle
└── 实况通知适配教程.md
```

---

## 二、API 版本要求详解

### 2.1 核心 API 版本对照表

| API | 方法/类 | 最低API版本 | 说明 |
|-----|---------|-------------|------|
| `NotificationChannel` | 通知渠道 | API 26 (Android 8.0) | 必须创建渠道才能显示通知 |
| `NotificationChannel.setImportance()` | 设置重要性 | API 26 | 控制通知打扰级别 |
| `NotificationManager.canPostPromotedNotifications()` | 检查实况通知权限 | API 36 (Android 15) | 检查应用是否可发布实况通知 |
| `Notification.Builder.setShortCriticalText()` | 状态栏简短文本 | API 36 | 在状态栏显示关键信息 |
| `NotificationCompat.Builder.setRequestPromotedOngoing()` | 请求提升为实况通知 | API 36 | 将通知提升到突出位置 |
| `NotificationManagerCompat.canPostPromotedNotifications()` | 兼容库检查方法 | API 36 | 通过兼容库检查权限 |
| `FOREGROUND_SERVICE_TYPE_DATA_SYNC` | 数据同步类型前台服务 | API 34 (Android 14) | 前台服务必须声明类型 |
| `POST_NOTIFICATIONS` | 通知发布权限 | API 33 (Android 13) | 运行时权限 |

### 2.2 版本判断逻辑

```java
// 项目中的版本判断实现
int sdkInt = Build.VERSION.SDK_INT;

// 实况通知可用性检查
boolean canShowLiveNotification = 
    sdkInt >= 36 &&                                          // 系统版本 >= Android 15
    ve2.f17454x7fb462b4.m16802xabb25d2e() &&                 // 用户设置已开启
    notificationManager.canPostPromotedNotifications();      // 系统权限已授予
```

---

## 三、前置条件详解

### 3.1 系统版本要求

```
最低支持版本: Android 8.0 (API 26)
实况通知支持: Android 15 (API 36)
```

**代码实现位置**: [nf1.java:60](file:///d:/appshare/app/src/main/java/p/nf1.java#L60)

```java
?? CanPostPromotedNotifications = Build.VERSION.SDK_INT >= 36 
    ? ((NotificationManager) this.$context.getSystemService(NotificationManager.class))
        .canPostPromotedNotifications() 
    : 0;
```

### 3.2 用户偏好设置

**存储位置**: `ve2.java` 类中的 `downloadLiveNotification` 属性

**属性索引**: `f17437x11d06cc6[79]` (数组中第79个元素)

**获取方法**: `m16802xabb25d2e()`

```java
// 用户设置检查
boolean userEnabled = ve2.f17454x7fb462b4.m16802xabb25d2e();
```

### 3.3 系统权限要求

#### 3.3.1 通知发布权限 (POST_NOTIFICATIONS)

**要求版本**: Android 13+ (API 33)

**权限类**: `PostNotificationsPermission`

**代码位置**: [am3.java:762-770](file:///d:/appshare/app/src/main/java/p/am3.java#L762)

```java
if (i >= 33) {
    d4 d4Var = new d4(fragmentActivity);
    IPermission iPermissionM16215xc98e9a30 = qx2.m16215xc98e9a30("android.permission.POST_NOTIFICATIONS");
    if (iPermissionM16215xc98e9a30 == null) {
        iPermissionM16215xc98e9a30 = new PostNotificationsPermission();
        qx2.f14515x7fb462b4.put("android.permission.POST_NOTIFICATIONS", iPermissionM16215xc98e9a30);
    }
    d4Var.m14349x7fb462b4(iPermissionM16215xc98e9a30);
    d4Var.m14348x11d06cc6(null);
}
```

#### 3.3.2 实况通知权限

**检查方法**: `NotificationManager.canPostPromotedNotifications()`

**代码位置**: [am3.java:772](file:///d:/appshare/app/src/main/java/p/am3.java#L772)

```java
if (i < 36 || ((NotificationManager) fragmentActivity.getSystemService(NotificationManager.class))
    .canPostPromotedNotifications()) {
    return;
}
mutableState2.setValue(Boolean.TRUE);  // 显示权限提示对话框
```

### 3.4 前台服务要求

**服务类**: `DownloadForegroundService`

**通知ID**: 1001 (服务前台通知)

**WakeLock时长**: 7200000ms (2小时)

---

## 四、通知渠道详解

### 4.1 渠道配置表

| 渠道ID | 渠道名称 | 重要性级别 | 数值 | 用途 |
|--------|----------|------------|------|------|
| `download_channel` | 下载通知 | IMPORTANCE_DEFAULT | 3 | 普通下载进度通知 |
| `download_live_channel` | 下载实况通知 | IMPORTANCE_HIGH | 4 | 实况通知（提升显示） |

### 4.2 普通下载通知渠道

**代码位置**: [of1.java:50-61](file:///d:/appshare/app/src/main/java/p/of1.java#L50)

```java
public static void m15837x11d06cc6(Context context) {
    NotificationManager notificationManager = (NotificationManager) context.getSystemService("notification");
    if (notificationManager.getNotificationChannel("download_channel") == null) {
        NotificationChannel notificationChannel = new NotificationChannel(
            "download_channel", 
            "下载通知", 
            3  // NotificationManager.IMPORTANCE_DEFAULT
        );
        notificationChannel.setDescription("应用下载进度通知");
        notificationChannel.setShowBadge(true);
        notificationChannel.enableLights(false);
        notificationChannel.enableVibration(false);
        notificationManager.createNotificationChannel(notificationChannel);
    }
}
```

### 4.3 实况通知渠道

**代码位置**: [of1.java:98-109](file:///d:/appshare/app/src/main/java/p/of1.java#L98)

```java
public static void m15840x7fb462b4(Context context) {
    NotificationManager notificationManager = (NotificationManager) context.getSystemService("notification");
    if (notificationManager.getNotificationChannel("download_live_channel") == null) {
        NotificationChannel notificationChannel = new NotificationChannel(
            "download_live_channel", 
            "下载实况通知", 
            4  // NotificationManager.IMPORTANCE_HIGH
        );
        notificationChannel.setDescription("在锁屏与状态栏展示更突出的下载状态");
        notificationChannel.setShowBadge(true);
        notificationChannel.enableLights(false);
        notificationChannel.enableVibration(false);
        notificationManager.createNotificationChannel(notificationChannel);
    }
}
```

### 4.4 服务启动时渠道创建

**代码位置**: [DownloadForegroundService.java:132-147](file:///d:/appshare/app/src/main/java/info/muge/appshare/service/DownloadForegroundService.java#L132)

```java
@Override
public final void onCreate() {
    super.onCreate();
    f3493xabb25d2e = this;
    NotificationManager notificationManager = (NotificationManager) getSystemService("notification");
    
    // 创建普通下载通知渠道
    NotificationChannel notificationChannel = new NotificationChannel(
        "download_channel", 
        "下载通知", 
        3
    );
    notificationChannel.setDescription("下载进度通知");
    notificationChannel.setShowBadge(true);
    notificationManager.createNotificationChannel(notificationChannel);
    
    // 创建实况通知渠道
    NotificationChannel notificationChannel2 = new NotificationChannel(
        "download_live_channel", 
        "下载实况通知", 
        4
    );
    notificationChannel2.setDescription("在锁屏与状态栏展示更突出的下载状态");
    notificationChannel2.setShowBadge(true);
    notificationChannel2.enableVibration(false);
    notificationChannel2.enableLights(false);
    notificationManager.createNotificationChannel(notificationChannel2);
    
    // 启动前台服务
    startForeground(1001, m13441x11d06cc6("下载服务启动中..."));
    
    // 获取WakeLock
    PowerManager.WakeLock wakeLockNewWakeLock = ((PowerManager) getSystemService("power"))
        .newWakeLock(1, "DownloadService::WakeLock");
    wakeLockNewWakeLock.acquire(7200000L);  // 2小时
    this.f3497x7fb462b4 = wakeLockNewWakeLock;
    
    // 注册网络回调
    ConnectivityManager connectivityManager = (ConnectivityManager) getSystemService("connectivity");
    NetworkRequest networkRequestBuild = new NetworkRequest.Builder()
        .addCapability(12)  // NET_CAPABILITY_VALIDATED
        .build();
    connectivityManager.registerNetworkCallback(networkRequestBuild, this.f3495xc9d8f452);
}
```

---

## 五、实况通知核心实现

### 5.1 下载进度通知构建

**代码位置**: [nf1.java:102-114](file:///d:/appshare/app/src/main/java/p/nf1.java#L102)

```java
// 判断是否使用实况通知
boolean z = Build.VERSION.SDK_INT >= 36 
    && ve2.f17454x7fb462b4.m16802xabb25d2e() 
    && canPostPromotedNotifications 
    && ((Number) obj).intValue() <= 1;  // 下载任务数 <= 1

String channelId = z ? "download_live_channel" : "download_channel";

// 创建通知Intent
Intent intent = new Intent(this.$context, (Class<?>) MainActivity.class);
intent.putExtra("activity", "DownloadList");
intent.addFlags(603979776);  // Intent.FLAG_ACTIVITY_NEW_TASK | FLAG_ACTIVITY_CLEAR_TASK | FLAG_ACTIVITY_TASK_ON_HOME
PendingIntent activity = PendingIntent.getActivity(this.$context, 0, intent, 201326592);

Intent intent2 = new Intent(this.$context, (Class<?>) DownloadForegroundService.class);
intent2.setAction("action_stop_download");
PendingIntent service = PendingIntent.getService(this.$context, 0, intent2, 67108864);

// 构建通知
NotificationCompat.Builder requestPromotedOngoing = new NotificationCompat.Builder(this.$context, channelId)
    .setContentTitle(this.$content + " 下载中...")
    .setContentText(this.$progress + "%")
    .setContentIntent(activity)
    .setSmallIcon(R.drawable.umeng_push_notification_default_small_icon)
    .setLargeIcon(BitmapFactory.decodeResource(this.$context.getResources(), R.mipmap.ic_launcher))
    .setProgress(100, this.$progress, false)
    .setOngoing(true)
    .setOnlyAlertOnce(true)
    .setDeleteIntent(service)
    .setCategory("progress")  // NotificationCompat.CATEGORY_PROGRESS
    .setVisibility(1)         // NotificationCompat.VISIBILITY_PUBLIC
    .setWhen(System.currentTimeMillis())
    .setRequestPromotedOngoing(z);  // 关键: 请求提升为实况通知

// 实况通知特有: 设置状态栏简短文本
if (z) {
    requestPromotedOngoing.setShortCriticalText(this.$progress + "%");
}

// 下载完成处理
if (this.$progress >= 100) {
    requestPromotedOngoing.setOngoing(false);
    requestPromotedOngoing.setOnlyAlertOnce(false);
    requestPromotedOngoing.setRequestPromotedOngoing(false);  // 取消实况状态
    requestPromotedOngoing.setContentTitle(this.$content + " 下载完成");
    requestPromotedOngoing.setAutoCancel(true);
}

notificationManager.notify(this.$notificationId, requestPromotedOngoing.build());
```

### 5.2 多任务下载通知

**代码位置**: [mf1.java:72-92](file:///d:/appshare/app/src/main/java/p/mf1.java#L72)

```java
int iIntValue = ((Number) obj).intValue();  // 正在下载的任务数

// 如果下载任务数 <= 1，取消多任务通知
if (iIntValue <= 1) {
    notificationManager.cancel(1002);
    return y44.f19200x7fb462b4;
}

// 判断是否使用实况通知
boolean z = Build.VERSION.SDK_INT >= 36 
    && ve2.f17454x7fb462b4.m16802xabb25d2e() 
    && notificationManager3.canPostPromotedNotifications();

String channelId = z ? "download_live_channel" : "download_channel";

// 构建多任务通知
NotificationCompat.Builder requestPromotedOngoing = new NotificationCompat.Builder(this.$context, channelId)
    .setSmallIcon(R.drawable.umeng_push_notification_default_small_icon)
    .setLargeIcon(BitmapFactory.decodeResource(this.$context.getResources(), R.mipmap.ic_launcher))
    .setContentTitle("正在下载 " + iIntValue + " 个任务")
    .setOnlyAlertOnce(true)
    .setContentIntent(PendingIntent.getActivity(this.$context, 0, intent, 201326592))
    .setOngoing(true)
    .setVisibility(1)
    .setCategory("progress")
    .setWhen(System.currentTimeMillis())
    .setRequestPromotedOngoing(z);

notificationManager.notify(1002, requestPromotedOngoing.build());
```

### 5.3 下载完成通知

**代码位置**: [of1.java:21-46](file:///d:/appshare/app/src/main/java/p/of1.java#L21)

```java
public static void m15836xc98e9a30(Context context, String str, String str2, int i) {
    NotificationManager notificationManager = (NotificationManager) context.getSystemService(NotificationManager.class);
    
    int i2 = Build.VERSION.SDK_INT;
    boolean z = i2 >= 36 
        && ve2.f17454x7fb462b4.m16802xabb25d2e() 
        && (i2 >= 36 ? notificationManager.canPostPromotedNotifications() : false);
    
    String channelId = z ? "download_live_channel" : "download_channel";
    
    // 创建安装Intent
    Intent intent = new Intent(context, (Class<?>) NotificationProxyActivity.class);
    intent.setAction("info.muge.appshare.ACTION_INSTALL_APK");
    intent.putExtra(UploadWorker.KEY_FILE_NAME, name);
    intent.putExtra("notification_id", i);
    intent.addFlags(335544320);  // Intent.FLAG_ACTIVITY_NEW_TASK | FLAG_ACTIVITY_CLEAR_TASK | FLAG_ACTIVITY_TASK_ON_HOME | FLAG_ACTIVITY_CLEAR_TOP
    
    NotificationCompat.Builder category = new NotificationCompat.Builder(context, channelId)
        .setContentTitle("下载完成")
        .setContentText(str2.concat(" 下载完成，点击安装"))
        .setContentIntent(PendingIntent.getActivity(context, i, intent, 201326592))
        .setSmallIcon(R.drawable.umeng_push_notification_default_small_icon)
        .setLargeIcon(BitmapFactory.decodeResource(context.getResources(), R.mipmap.ic_launcher))
        .setAutoCancel(true)
        .setVisibility(1)
        .setCategory("status");
    
    // 实况通知特有设置
    if (z) {
        category.setOngoing(true);
        category.setRequestPromotedOngoing(true);
        category.setShortCriticalText("点击安装");
        category.setAutoCancel(false);
    }
    
    notificationManager.notify(i, category.build());
}
```

### 5.4 状态通知（连接中/暂停/失败等）

**代码位置**: [of1.java:71-95](file:///d:/appshare/app/src/main/java/p/of1.java#L71)

```java
public static void m15839xb0e30dd6(Context context, String str, String str2, int i, boolean z, boolean z2) {
    NotificationManager notificationManager = (NotificationManager) context.getSystemService(NotificationManager.class);
    
    int i2 = Build.VERSION.SDK_INT;
    boolean z3 = i2 >= 36 
        && ve2.f17454x7fb462b4.m16802xabb25d2e() 
        && (i2 >= 36 ? notificationManager.canPostPromotedNotifications() : false);
    
    String channelId = z3 ? "download_live_channel" : "download_channel";
    
    NotificationCompat.Builder requestPromotedOngoing = new NotificationCompat.Builder(context, channelId)
        .setSmallIcon(R.drawable.umeng_push_notification_default_small_icon)
        .setLargeIcon(BitmapFactory.decodeResource(context.getResources(), R.mipmap.ic_launcher))
        .setContentTitle(str2 + " " + str)  // "应用名 状态"
        .setOnlyAlertOnce(!z2)
        .setContentIntent(PendingIntent.getActivity(context, 0, intent, 201326592))
        .setOngoing(z)
        .setVisibility(1)
        .setCategory("status")
        .setWhen(System.currentTimeMillis())
        .setRequestPromotedOngoing(z3);
    
    if (z3) {
        requestPromotedOngoing.setShortCriticalText(str);
        requestPromotedOngoing.setOngoing(true);
    }
    
    notificationManager.notify(i, requestPromotedOngoing.build());
}
```

---

## 六、NotificationCompat API 详解

### 6.1 setRequestPromotedOngoing()

**代码位置**: [NotificationCompat.java:2160-2163](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationCompat.java#L2160)

```java
public Builder setRequestPromotedOngoing(boolean z) {
    getExtras().putBoolean(NotificationCompat.EXTRA_REQUEST_PROMOTED_ONGOING, z);
    return this;
}
```

**功能**: 请求将通知提升为实况通知

**参数**:
- `z`: `true` 表示请求提升，`false` 表示取消提升

**生效条件**:
1. 系统版本 >= API 36 (Android 15)
2. 应用已获得实况通知权限
3. 通知为持续通知 (`setOngoing(true)`)

### 6.2 setShortCriticalText()

**代码位置**: [NotificationCompat.java:2170-2176](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationCompat.java#L2170)

```java
public Builder setShortCriticalText(String str) {
    this.mShortCriticalText = str;
    if (Build.VERSION.SDK_INT < 36) {
        getExtras().putString(NotificationCompat.EXTRA_SHORT_CRITICAL_TEXT, str);
    }
    return this;
}
```

**功能**: 设置状态栏显示的简短关键文本

**参数**:
- `str`: 要显示的简短文本（如 "50%"、"点击安装"）

**显示位置**: 状态栏通知图标旁边

**生效条件**: 系统版本 >= API 36

### 6.3 NotificationCompatBuilder 构建

**代码位置**: [NotificationCompatBuilder.java:372-374](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationCompatBuilder.java#L372)

```java
if (i6 >= 36) {
    Api36Impl.setShortCriticalText(this.mBuilder, builder.mShortCriticalText);
}
```

### 6.4 Api36Impl 实现

**代码位置**: [NotificationCompatBuilder.java:241-248](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationCompatBuilder.java#L241)

```java
@RequiresApi(36)
public static final class Api36Impl {
    private Api36Impl() {
    }

    public static Notification.Builder setShortCriticalText(Notification.Builder builder, String str) {
        return builder.setShortCriticalText(str);
    }
}
```

### 6.5 NotificationManagerCompat.canPostPromotedNotifications()

**代码位置**: [NotificationManagerCompat.java:484-489](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationManagerCompat.java#L484)

```java
public boolean canPostPromotedNotifications() {
    if (Build.VERSION.SDK_INT >= 36) {
        return Api36Impl.canPostPromotedNotifications(this.mNotificationManager);
    }
    return false;
}
```

### 6.6 Api36Impl 权限检查

**代码位置**: [NotificationManagerCompat.java:148-156](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationManagerCompat.java#L148)

```java
@RequiresApi(36)
public static class Api36Impl {
    private Api36Impl() {
    }

    public static boolean canPostPromotedNotifications(NotificationManager notificationManager) {
        return notificationManager.canPostPromotedNotifications();
    }
}
```

---

## 七、前台服务实现

### 7.1 DownloadForegroundService 类结构

```java
@StabilityInferred(parameters = 0)
public final class DownloadForegroundService extends Service {
    
    // 静态实例引用
    public static DownloadForegroundService f3493xabb25d2e;
    
    // 网络回调
    public qf1 f3495xc9d8f452;
    
    // 连接管理器
    public ConnectivityManager f3496x11d06cc6;
    
    // WakeLock
    public PowerManager.WakeLock f3497x7fb462b4;
    
    // Companion对象（通知渠道管理）
    public static final of1 Companion = new of1();
    
    // 原子计数器
    public static final AtomicInteger f3491xc98e9a30 = new AtomicInteger(0);
    public static final AtomicInteger f3494xb0e30dd6 = new AtomicInteger(0);
    public static final AtomicInteger f3492xe052fdc6 = new AtomicInteger(0);
}
```

### 7.2 服务通知构建

**代码位置**: [DownloadForegroundService.java:111-124](file:///d:/appshare/app/src/main/java/info/muge/appshare/service/DownloadForegroundService.java#L111)

```java
public final Notification m13441x11d06cc6(String str) {
    Intent intent = new Intent(this, (Class<?>) MainActivity.class);
    intent.putExtra("activity", "DownloadList");
    intent.addFlags(603979776);
    PendingIntent activity = PendingIntent.getActivity(this, 0, intent, 201326592);
    
    Intent intent2 = new Intent(this, (Class<?>) DownloadForegroundService.class);
    intent2.setAction("action_stop_download");
    PendingIntent service = PendingIntent.getService(this, 0, intent2, 67108864);
    
    Companion.getClass();
    of1.m15837x11d06cc6(this);  // 确保渠道存在
    
    Notification notificationBuild = new NotificationCompat.Builder(this, "download_channel")
        .setContentTitle("下载服务")
        .setContentText(str)
        .setSmallIcon(R.drawable.umeng_push_notification_default_small_icon)
        .setLargeIcon(BitmapFactory.decodeResource(getResources(), R.mipmap.ic_launcher))
        .setContentIntent(activity)
        .setOngoing(true)
        .setCategory("service")
        .setShortCriticalText(str)
        .setRequestPromotedOngoing(false)  // 服务通知不提升
        .setOnlyAlertOnce(true)
        .setDeleteIntent(service)
        .setPriority(-1)  // PRIORITY_LOW
        .build();
    
    return notificationBuild;
}
```

### 7.3 服务生命周期

```
onCreate()
    ├── 创建通知渠道 (download_channel, download_live_channel)
    ├── startForeground(1001, notification)
    ├── 获取 WakeLock (2小时超时)
    ├── 注册网络回调
    └── 重置计数器

onStartCommand(Intent, int, int)
    └── 启动协程处理下载任务

onDestroy()
    ├── 清除静态引用
    ├── 释放 WakeLock
    └── 注销网络回调
```

---

## 八、下载任务管理

### 8.1 DownloadTaskManager 类

**代码位置**: [DownloadTaskManager.java](file:///d:/appshare/app/src/main/java/info/muge/appshare/utils/download/core/DownloadTaskManager.java)

### 8.2 通知更新逻辑

**代码位置**: [DownloadTaskManager.java:315-362](file:///d:/appshare/app/src/main/java/info/muge/appshare/utils/download/core/DownloadTaskManager.java#L315)

```java
private final void notifyUpdate(DownloadRecord downloadRecord, int i) {
    this.onUpdate.invoke(downloadRecord, Integer.valueOf(i));
    int iAbs = Math.abs(downloadRecord.getId().hashCode());
    Context applicationContext = this.mDownloadConfig.f14143x7fb462b4.getApplicationContext();
    
    switch (i) {
        case 1:  // 下载开始
        case 2:  // 进度更新
            int currentLength = downloadRecord.getTotalLength() > 0 
                ? (int) ((downloadRecord.getCurrentLength() * 100) / downloadRecord.getTotalLength()) 
                : 0;
            if (currentLength > 100) currentLength = 100;
            // 调用 nf1 构建进度通知
            yn4.m17169x78547bd2(
                l31.m15517x7fb462b4(af2.f3982x7fb462b4), 
                null, null, 
                new nf1(applicationContext, name, currentLength, iAbs, null), 
                3
            );
            break;
            
        case 3:  // 暂停/取消
            String str = downloadRecord.getStatus() == DownloadRecord.Status.PAUSED 
                ? "已暂停" : "已取消";
            of1.m15839xb0e30dd6(applicationContext, str, name2, iAbs, false, false);
            break;
            
        case 5:  // 连接中
            of1.m15839xb0e30dd6(applicationContext, "连接中", name3, iAbs, true, false);
            break;
            
        case 6:  // 下载失败
            of1.m15839xb0e30dd6(applicationContext, "下载失败", name4, iAbs, false, false);
            break;
            
        case 7:  // 准备下载
            of1.m15839xb0e30dd6(applicationContext, "准备下载", name5, iAbs, true, false);
            break;
    }
}
```

### 8.3 下载状态枚举

| 状态值 | 状态名称 | 通知类型 |
|--------|----------|----------|
| 1 | DOWNLOADING | 进度通知 |
| 2 | 进度更新 | 进度通知 |
| 3 | PAUSED/CANCELLED | 状态通知 |
| 5 | CONNECTING | 状态通知（持续） |
| 6 | ERROR | 状态通知 |
| 7 | PREPARING | 状态通知（持续） |

### 8.4 进度更新节流

**代码位置**: [DownloadTaskManager.java:533-573](file:///d:/appshare/app/src/main/java/info/muge/appshare/utils/download/core/DownloadTaskManager.java#L533)

```java
public synchronized void onProgressChanged(int i, int i2) {
    // 更新下载进度
    if (this.mDownloadEntry.isSupportRange()) {
        HashMap<Integer, Integer> ranges = this.mDownloadEntry.getRanges();
        Integer num = ranges2.get(Integer.valueOf(i));
        ranges.put(Integer.valueOf(i), Integer.valueOf((num != null ? num.intValue() : 0) + i2));
    }
    
    DownloadRecord downloadRecord = this.mDownloadEntry;
    downloadRecord.setCurrentLength(downloadRecord.getCurrentLength() + ((long) i2));
    
    // 进度节流：500ms 最小间隔
    synchronized (d04Var) {
        long jCurrentTimeMillis = System.currentTimeMillis();
        if (jCurrentTimeMillis - d04Var.f5542x7fb462b4 > 500) {
            d04Var.f5542x7fb462b4 = jCurrentTimeMillis;
            z = true;  // 允许更新通知
        }
    }
    
    if (z) {
        notifyUpdate(this.mDownloadEntry, 2);
    }
}
```

---

## 九、用户设置界面

### 9.1 设置开关实现

**代码位置**: [am3.java:755-777](file:///d:/appshare/app/src/main/java/p/am3.java#L755)

```java
public static void m13950x7fb462b4(FragmentActivity fragmentActivity, MutableState mutableState, MutableState mutableState2, boolean z) {
    // 更新UI状态
    mutableState.setValue(Boolean.valueOf(z));
    
    // 保存用户设置
    ve2 ve2Var = ve2.f17454x7fb462b4;
    ve2.X.m16646xcf029ab4(ve2Var, ve2.f17437x11d06cc6[79], Boolean.valueOf(z));
    
    if (z) {
        int i = Build.VERSION.SDK_INT;
        
        // Android 13+ 请求通知权限
        if (i >= 33) {
            d4 d4Var = new d4(fragmentActivity);
            IPermission iPermissionM16215xc98e9a30 = qx2.m16215xc98e9a30("android.permission.POST_NOTIFICATIONS");
            if (iPermissionM16215xc98e9a30 == null) {
                iPermissionM16215xc98e9a30 = new PostNotificationsPermission();
                qx2.f14515x7fb462b4.put("android.permission.POST_NOTIFICATIONS", iPermissionM16215xc98e9a30);
            }
            d4Var.m14349x7fb462b4(iPermissionM16215xc98e9a30);
            d4Var.m14348x11d06cc6(null);
        }
        
        // Android 15+ 检查实况通知权限
        if (i < 36 || ((NotificationManager) fragmentActivity.getSystemService(NotificationManager.class))
            .canPostPromotedNotifications()) {
            return;
        }
        
        // 显示权限提示对话框
        mutableState2.setValue(Boolean.TRUE);
    }
}
```

### 9.2 设置项定义

**代码位置**: [ve2.java](file:///d:/appshare/app/src/main/java/p/ve2.java)

```java
// 用户设置属性数组
public static final /* synthetic */ KProperty[] f17437x11d06cc6 = {
    // ... 其他设置项 ...
    // 索引 79: downloadLiveNotification
};

// 获取实况通知设置
public final boolean m16802xabb25d2e() {
    return ((Boolean) mutableState7.getValue()).booleanValue();
}
```

---

## 十、通知ID分配

| ID | 用途 | 说明 |
|----|------|------|
| 1001 | 前台服务通知 | DownloadForegroundService 服务通知 |
| 1002 | 多任务下载通知 | 同时下载多个任务时显示 |
| Math.abs(id.hashCode()) | 单任务下载通知 | 根据下载ID哈希生成唯一ID |

---

## 十一、PendingIntent Flags

| Flag值 | 数值 | 说明 |
|--------|------|------|
| FLAG_UPDATE_CURRENT | 134217728 (0x08000000) | 更新现有PendingIntent |
| FLAG_IMMUTABLE | 67108864 (0x04000000) | 不可变PendingIntent |
| FLAG_CANCEL_CURRENT | 268435456 (0x10000000) | 取消现有PendingIntent |
| 组合值 (UPDATE_CURRENT \| IMMUTABLE) | 201326592 | 常用组合 |

---

## 十二、Intent Flags

| Flag值 | 数值 | 说明 |
|--------|------|------|
| FLAG_ACTIVITY_NEW_TASK | 268435456 (0x10000000) | 新任务启动 |
| FLAG_ACTIVITY_CLEAR_TASK | 33554432 (0x02000000) | 清除任务栈 |
| FLAG_ACTIVITY_TASK_ON_HOME | 16384 (0x00004000) | 在HOME上启动 |
| FLAG_ACTIVITY_CLEAR_TOP | 536870912 (0x20000000) | 清除顶部Activity |
| 组合值 | 603979776 | NEW_TASK \| CLEAR_TASK \| TASK_ON_HOME |
| 组合值 | 335544320 | NEW_TASK \| CLEAR_TASK \| TASK_ON_HOME \| CLEAR_TOP |

---

## 十三、通知类别常量

| 类别 | 常量值 | 用途 |
|------|--------|------|
| CATEGORY_PROGRESS | "progress" | 进度通知 |
| CATEGORY_STATUS | "status" | 状态通知 |
| CATEGORY_SERVICE | "service" | 服务通知 |

---

## 十四、可见性常量

| 可见性 | 数值 | 说明 |
|--------|------|------|
| VISIBILITY_PUBLIC | 1 | 锁屏完全可见 |
| VISIBILITY_PRIVATE | 0 | 锁屏显示基本信息 |
| VISIBILITY_SECRET | -1 | 锁屏不可见 |

---

## 十五、重要性级别

| 级别 | 数值 | 说明 |
|------|------|------|
| IMPORTANCE_UNSPECIFIED | -1000 | 未指定 |
| IMPORTANCE_NONE | 0 | 不显示通知 |
| IMPORTANCE_MIN | 1 | 最低重要性 |
| IMPORTANCE_LOW | 2 | 低重要性 |
| IMPORTANCE_DEFAULT | 3 | 默认重要性 |
| IMPORTANCE_HIGH | 4 | 高重要性 |
| IMPORTANCE_MAX | 5 | 最高重要性 |

---

## 十六、错误处理与边界情况

### 16.1 版本兼容处理

```java
// 所有实况通知API调用前必须进行版本检查
if (Build.VERSION.SDK_INT >= 36) {
    builder.setRequestPromotedOngoing(true);
    builder.setShortCriticalText(progress + "%");
}
```

### 16.2 权限缺失处理

```java
// 检查权限后显示引导对话框
if (!notificationManager.canPostPromotedNotifications()) {
    mutableState2.setValue(Boolean.TRUE);  // 显示权限引导
}
```

### 16.3 下载完成清理

```java
// 下载完成时取消实况状态
if (progress >= 100) {
    builder.setRequestPromotedOngoing(false);
    builder.setAutoCancel(true);
}
```

---

## 十七、测试检查清单

### 17.1 基础功能测试

- [ ] Android 8.0-14 设备上普通通知正常显示
- [ ] Android 15+ 设备上实况通知正常显示
- [ ] 状态栏显示进度百分比
- [ ] 锁屏界面显示下载进度
- [ ] 下载完成通知可点击安装

### 17.2 权限测试

- [ ] 首次开启实况通知时请求通知权限
- [ ] 系统设置关闭实况通知后降级为普通通知
- [ ] 应用内关闭实况通知后使用普通通知

### 17.3 边界情况测试

- [ ] 多任务同时下载时显示汇总通知
- [ ] 单任务时显示详细进度通知
- [ ] 下载暂停/取消后通知状态正确
- [ ] 下载失败后通知状态正确
- [ ] 网络断开后通知状态正确

---

## 十八、相关文件索引

| 文件路径 | 功能 |
|----------|------|
| [nf1.java](file:///d:/appshare/app/src/main/java/p/nf1.java) | 下载进度通知构建 |
| [mf1.java](file:///d:/appshare/app/src/main/java/p/mf1.java) | 多任务下载通知构建 |
| [of1.java](file:///d:/appshare/app/src/main/java/p/of1.java) | 通知渠道管理 |
| [am3.java](file:///d:/appshare/app/src/main/java/p/am3.java) | 设置界面逻辑 |
| [ve2.java](file:///d:/appshare/app/src/main/java/p/ve2.java) | 用户设置存储 |
| [ze2.java](file:///d:/appshare/app/src/main/java/p/ze2.java) | 通知取消处理 |
| [DownloadForegroundService.java](file:///d:/appshare/app/src/main/java/info/muge/appshare/service/DownloadForegroundService.java) | 下载前台服务 |
| [DownloadTaskManager.java](file:///d:/appshare/app/src/main/java/info/muge/appshare/utils/download/core/DownloadTaskManager.java) | 下载任务管理 |
| [NotificationCompat.java](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationCompat.java) | 通知兼容库 |
| [NotificationCompatBuilder.java](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationCompatBuilder.java) | 通知构建器 |
| [NotificationManagerCompat.java](file:///d:/appshare/app/src/main/java/androidx/core/app/NotificationManagerCompat.java) | 通知管理器兼容 |

---

## 十九、参考资源

- [Android 15 行为变更](https://developer.android.com/about/versions/15/behavior-changes-15)
- [NotificationCompat.Builder 文档](https://developer.android.com/reference/androidx/core/app/NotificationCompat.Builder)
- [前台服务类型](https://developer.android.com/about/versions/14/changes/fgs-types-required)
- [通知渠道](https://developer.android.com/develop/ui/views/notifications/channels)

---

**文档版本**: 1.0  
**最后更新**: 2026-02-26  
**基于项目版本**: AppShare 5.0.8 (versionCode: 403)
